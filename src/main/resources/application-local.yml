spring:
  config:
    import: datasource.yml, secret.yml
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true

    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true

logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
